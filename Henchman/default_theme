# Default theme (format and color) for vcs

# colors!
# You can use names or numbers. I like names.
zstyle ':henchman:vcs:color:' branch   magenta
zstyle ':henchman:vcs:color:' staged   red
zstyle ':henchman:vcs:color:' unstaged blue
zstyle ':henchman:vcs:color:' clean    green
zstyle ':henchman:vcs:color:' action   cyan

zstyle ':henchman:vcs:format:' \* '(%b%f%u%c)'
zstyle ':henchman:vcs:actionformat:' \* '(%b%f:%a%u%c)'
zstyle ':henchman:vcs:*:' prefix '[%s]-'
zstyle ':henchman:vcs:nvcsformats:' \* '%#'

# smart marks:
# We specify width, since some terminals get upset otherwise
zstyle ':henchman:vcs:mark' \*     '[%s]%1{ᴤ%}'  # default/unknown
zstyle ':henchman:vcs:mark' bzr    '%2{⚯ %}' # Bazaar      <http://bazaar-vcs.org/>
zstyle ':henchman:vcs:mark' cvs    '%1{☤%%}' # Concurrent Versioning System
                                             #             <http://www.nongnu.org/cvs/>
zstyle ':henchman:vcs:mark' darcs  '%2{✼ %}' # Darcs       <http://darcs.net/>
zstyle ':henchman:vcs:mark' fossil '%2{⌘ %}' # Fossil      <http://fossil-scm.org/>
zstyle ':henchman:vcs:mark' git    '%1{±%}'  # Git         <http://git-scm.com/>
zstyle ':henchman:vcs:mark' hg     '%1{☿%}'  # Mercurial   <http://mercurial.selenic.com/>
zstyle ':henchman:vcs:mark' p4     '%1{ꟼ%}'  # Perforce    <http://www.perforce.com/>
zstyle ':henchman:vcs:mark' svn    '%1{‡%}'  # Subversion  <http://subversion.tigris.org/>
zstyle ':henchman:vcs:mark' tla    '%2{⌒ %}' # GNU arch    <http://www.gnu.org/software/gnu-arch/>
# zstyle ':henchman:vcs:*' mark cdv ''           # Codeville   <http://codeville.org/>
# zstyle ':henchman:vcs:*' mark mtn '%2{🐭 %}'    # Monotone    <http://monotone.ca/>
# zstyle ':henchman:vcs:*' mark svk ''           # SVK         <http://svk.bestpractical.com/>

PS1='%F{5}[%F{2}%n%F{5}]%f %F{3}%3~%f ${vcs_info_msg_1_}%B${vcs_info_msg_0_}%b '

# vim:ft=zsh:ts=4:sw=4:et
